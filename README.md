# Planning Poker 

Install and running method's:
- Install dependencies: 
    ```
    npm install
    ```
    or
    ```
    yarn
    ```
- Rename ```.env.example``` to ```.env``` and change values  
- Run application 
    ```
    npm run start
    ```
    or
    ```
    yarn start
    ```